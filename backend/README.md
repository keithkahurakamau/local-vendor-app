Backend setup
=============

Environment
-----------
Copy `backend/.env.example` to `backend/.env` and fill in real values (database credentials, API keys, secrets) before running the backend.

Important: `backend/.env` should not be committed; keep secrets in your local env or a secrets manager.

Example commands:

```bash
cp backend/.env.example backend/.env
# edit backend/.env
```
